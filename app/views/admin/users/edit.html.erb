<div class="mx-auto max-w-2xl rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8">
  <h1 class="text-3xl font-bold">Edit user</h1><p class="mt-2 mb-6 text-slate-500">Leave password fields blank to keep the current password.</p>
  <%= render "shared/user_form", user: @user, url: admin_user_path(@user), submit_label: "Save user", allow_role: true %>
</div>
